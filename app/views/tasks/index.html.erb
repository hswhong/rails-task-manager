<h1>Task List</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<h3>Create new task!</h3>
<div class="btn btn-primary">
  <%= link_to "Click here to create task", tasks_new_path, :class => "btn" %>
</div>
